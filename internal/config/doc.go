// Package config defines the explicit runtime configuration used by Fornix.
// Configuration is intentionally environment-driven so credentials and deploy-
// specific values do not become source-controlled application state.
package config
