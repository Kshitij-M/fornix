# Fornix reference repository

This small fixture is used by the deterministic operator workflow smoke. The
workflow indexes it, retrieves bounded context, asks the fake provider to read
this file through the registered read-only structured-argv tool, and stores a
hash-addressed report.
